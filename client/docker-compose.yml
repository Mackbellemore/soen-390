version: '3.8'
services:
  client:
    stdin_open: true
    container_name: client
    restart: always
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - './:/app'
      - '/app/.next'
      - '/app/node_modules'
